{
  "name": "jpl",
  "version": "0.1.1",
  "description": "Command line JSON parser, processor",
  "main": "index.js",
  "scripts": {
    "pretest": "`npm bin`/eslint src/ bin/ tests/",
    "test": "`npm bin`/nyc `npm bin`/ava tests/",
    "test:cov": "`npm bin`/nyc report --reporter=html `npm bin`/ava tests/"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "bin": {
    "jpl": "./bin/index.js"
  },
  "author": "Brandon Wood",
  "repository": "https://github.com/woodb/jpl.git",
  "license": "MIT",
  "dependencies": {
    "commander": "^2.11.0"
  },
  "devDependencies": {
    "ava": "^0.22.0",
    "eslint": "^4.9.0",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.8.0",
    "nyc": "^11.2.1"
  }
}
